export class Track {
  id: number;
  title: string;
  duration: string;


  constructor(id: number, title: string, duration: string) {
    this.id = id
    this.title = title
    this.duration = duration
  }

}
